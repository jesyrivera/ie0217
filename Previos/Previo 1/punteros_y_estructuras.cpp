#include <iostream>
using namespace std;

struct temp { //se define una estructura que tiene dos miembros
    int i;
    float f;
};

int main() {
    temp *ptr; //se declara un puntero ptr
    return 0;
}