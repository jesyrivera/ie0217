#include <iostream>
using namespace std;

int main () {
    //se inicia el ciclo que se va a ejecutar mientra la variable sea menor o igual a 5, se va aumentando
    for (int i = 1; i <= 5; ++i){ 
        cout << i << " ";//imprime el valor actual del ciclo
    }
    return 0;
}